import wollok.game.*
import pepita.*
import texto.*
import nivel.*

program juego {
	game.title("Matando a pepita wacho")
	nivel.configuracionInicial()
	game.addVisual(pepita)
	game.addVisual(texto)
	game.start()
}