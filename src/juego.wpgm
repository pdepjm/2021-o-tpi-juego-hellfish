import wollok.game.*
import pepita.*
import texto.*
import enemigo.*

program juego {
	game.title("Juego Base")
	game.addVisual(pepita)
	game.addVisual(new Enemigo(bola=bola0))
	game.addVisual(texto)
	game.start()
}